# Copy this file to .env and populate it before running LLM-as-a-judge evaluation (ADR 0007).
# Granite itself is not gated, so quantization and local evaluation need no secrets.
NVIDIA_API_KEY=""
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
JUDGE_MODEL=""
